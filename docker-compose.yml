services:
  db:
    image: postgres:16
    container_name: pampa-postgres
    environment:
      POSTGRES_USER: pampa
      POSTGRES_PASSWORD: pampa
      POSTGRES_DB: pampa
    ports:
      - "5432:5432"
    volumes:
      - pampa_pgdata:/var/lib/postgresql/data

volumes:
  pampa_pgdata:

